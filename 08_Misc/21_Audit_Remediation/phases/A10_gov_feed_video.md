# A10. Gov-feed Unlisted YouTube (D1)

Prepend `00_MASTER_CONTEXT.md`. HUMAN (Lead + Arnav). Requires A08.

## Goal

Evaluation area 1. Live catalogue camera, not CAM-OWN-001, not a wget mp4.

## HUMAN

1. App running. `SENTINEL_HOST` set. Sync if the table lacks `cam01`–`cam30`.
2. Follow `05_Output/deliverables/gov_feed_demo_script.md`.
3. Open `cam04` from the Cameras table. MUST say the catalogue has no GIS.
4. Confirm `GJ01AB1234` on `cam04` unless a new OCR row exists. MUST say confirm if confidence is 1.0 from confirm.
5. Reconstruct. Show seeds plus append. Download CSV.
6. ≤3 min. Unlisted YouTube. Incognito play.
7. URL into `SUBMISSION_PACK.md` row Gov-feed YouTube.

## Agent

Do not wget `/stream/<id>`. Do not open more than 2 live tiles. If asked to call a fixture camera a government feed, refuse.

## Done when

- Incognito URL works.
- CSV A10-001 DONE.

## Do not

Email SCRB because of SETUP 461. That is UDP then TCP.
