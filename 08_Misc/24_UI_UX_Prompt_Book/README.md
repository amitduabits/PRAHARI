# PRAHARI UI/UX prompt book

The current SOC UI looks generated (navy + gold + tracked kicker + Segoe + placeholder labels + JSON dumps). This book locks a **duty-desk** direction and a **pass/fail number bar** so an agent cannot “make it nicer” into a second generic skin.

Open `START_HERE.md`.
