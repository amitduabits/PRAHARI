# U02 — Shell: login, header, tabs, footer

AGENT. Depends on U01.

## Goal

Duty-desk chrome. 48 px navy header, khaki 3 px stripe, tabs with underline, Log in.

## Agent

1. Login: labelled User / Password. Button **Log in**. Error `role="alert"`. Hint does not lead with `.env`.
2. Header: wordmark PRAHARI 18 px; `Home Department · SOC` 12 px; chip `Hybrid 1+2+thin 3`; health as chips (cameras · detections · WS), not a manifesto paragraph.
3. Tabs: `role="tab"` `aria-selected`. Selected = 3 px khaki underline, ink text, **no gold fill**.
4. Panels: `role="tabpanel"`.
5. Footer: keep DESIGN TARGET + 80,000 + four open tiles. 12 px `--muted`.
6. Tick U02-*.

## Done when

Screenshot at 1280 px: no uppercase tracked kicker, no gold selected tab. `test_honesty` still green.
