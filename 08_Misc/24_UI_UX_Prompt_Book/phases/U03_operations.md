# U03 — Operations (ten-foot)

AGENT. Depends on U02.

## Goal

Map + 2×2 wall readable from a video recording. Fifth tile error is loud.

## Agent

1. Split 58 / 42 at ≥ 1280 px. Map `min-height: 420px` (or 56vh, cap 640 px).
2. Map `aria-label="Gujarat camera map"`. Pins keep health colours via `--live / --warn / --offline`.
3. Tile: 12 px id, Close text button, video `object-fit: contain`, black letterbox.
4. Empty copy stays: click pin or table; own_feed path for CAM-OWN-001.
5. 429 copy stays: “Fifth live session rejected. Close a tile first.”
6. Tick U03-001.

## Do not

Add a third column. Auto-play sound. OSM attribution may remain.
