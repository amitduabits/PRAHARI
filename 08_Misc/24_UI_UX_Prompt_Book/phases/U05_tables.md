# U05 — Cameras and Track

AGENT. Depends on U02.

## Goal

Tables that look like a roster, not cards.

## Agent

1. `thead` sticky. Row height `--row`. Mono for `camera_id` and plates.
2. Cameras: caption about no coordinates stays. Open is the last column. Filters are text chips, selected = underline.
3. Track: plate input labelled. Reconstruct, Next cameras, CSV link in one toolbar. Meta line: `N points · category`. Map under the table or beside at 1280.
4. Predict JSON may stay in a closed `<details>` so it is not a wall of pre.
5. Tick U05-001.

## Do not

Replace GIS plate track with ByteTrack UI.
