# C14 — Incognito, portal, freeze (HUMAN)

Prepend `00_MASTER_CONTEXT.md`. HUMAN. Agent only refreshes local preflight.

## Goal

Every submitted URL works logged-out. Portal receipt exists before 07 September 2026 12:00 IST. Product edits stop.

## HUMAN (Lead)

1. Incognito checklist (tick in SUBMISSION_PACK.md):
   - Own-feed YouTube
   - Gov-feed YouTube
   - Drive CSV
   - GitHub README clone-and-run
   - Solution PDF / slides PDF
   - Hosted URL if any (login judge)
2. Optional hosted URL: only if `audit_gate.py` PASS and passwords rotated. Do not put RTSP on the public internet.
3. Portal form: Academic / student. Attach PPT/PDF, HLD, both videos, Drive, GitHub, optional host+login. Submit **before noon IST**.
4. Screenshot `07_Communications/submission_receipt.png`.
5. Stop CSS and feature work. Shortlisting is that evening.

## Agent

1. `scripts/incognito_preflight.ps1` on local APIs.
2. Dump latest `track_GJ01AB1234.csv` to deliverables.
3. Push `main` (arnav) without `.env`.
4. List HUMAN leftovers. Do not start new engines after the receipt.

## Finale bag (after shortlisting, still this phase)

Two laptops, `prahari.db`, `own_feed.mp4`, HDMI/dongle, chargers, printed architecture, FINALE_RUNCARD.md. Designated plate at iHub may differ: use confirm.

## Done when

- receipt png exists.
- CSV C14-003 DONE.
- No further product commits unless a submitted link is 403.

## Do not

Miss noon. Change JUDGE_PASSWORD after sending credentials to the committee without emailing them. Rebuild P00.
