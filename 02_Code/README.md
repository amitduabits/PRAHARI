# Code

The application tree is `prahari/`.

```powershell
cd prahari
copy .env.example .env
.\run.ps1
```

Open http://127.0.0.1:8080 and sign in as `judge`. Standing product rules are in `../../04_Documents/PRAHARI_HLD.md` and the root `README.md`.
