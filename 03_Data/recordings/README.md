# Recordings

Drop `own_feed.mp4` here (Aria, P04-010). Minimum ~2 minutes of public-road video with readable plates if possible.

Do not place Sentinel sandbox dumps here. The integrator guide forbids treating `/stream/<id>` as a file download.
